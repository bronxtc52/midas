services:
  midas:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
    restart: always
    logging:
      driver: json-file
      options:
        max-size: "10m"
